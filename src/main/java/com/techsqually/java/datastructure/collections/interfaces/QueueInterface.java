package com.techsqually.java.datastructure.collections.interfaces;

public class QueueInterface {

  /**
   * 2. element() -> returns the element at the head of the queue. The element is not removed.
   *                 it throws NoSuchElementException if the queue is empty
   * 3. offer() -> add element to the queue. Returns true if obj was added and false otherwise
   *    peek() -> null if empty /element() Exception if empty  -> Returns the element at the head of the queu. It returns null if empty. the element is not removed
   *    poll() -> Returns the element at the head of the queue, removing the element in the process
   *              returns null if the queue is empty
   *    remove() -> remove the element at the head of the queue. Throws NoSuchElementException if the queue isempty
   *
   * */

}
